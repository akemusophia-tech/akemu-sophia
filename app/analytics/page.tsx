import { BarChart3, Clock3, MessageCircle, Sparkles, TrendingUp } from 'lucide-react'
import { PageHeader, SupportShell } from '@/components/support-shell'

export default function AnalyticsPage() { return <SupportShell active="Analytics"><section className="px-4 py-7 sm:px-6 lg:px-8 lg:py-9"><PageHeader eyebrow="Performance overview" title="Analytics" description="Understand how your team is supporting customers." action={<select className="min-h-11 rounded-xl border border-input bg-card px-3 text-sm font-semibold"><option>Last 30 days</option><option>Last 7 days</option><option>This quarter</option></select>} /><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Kpi icon={MessageCircle} label="Tickets received" value="248" change="+12.4%" /><Kpi icon={Clock3} label="Avg. response time" value="18m" change="-8.2%" /><Kpi icon={TrendingUp} label="Resolution rate" value="94.6%" change="+4.1%" /><Kpi icon={Sparkles} label="AI-assisted" value="67%" change="+9.8%" /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.45fr_1fr]"><ChartCard title="Ticket volume" detail="Incoming requests and resolved tickets"><div className="flex h-64 items-end gap-2 border-b border-l border-border px-3 pb-0 pt-8">{[42, 58, 48, 72, 62, 86, 76, 92, 68, 78, 96, 82, 88, 100].map((height, i) => <div key={i} className="group flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-md bg-primary/70 transition group-hover:bg-primary" style={{ height: `${height}%` }} /><span className="text-[10px] text-muted-foreground">{i + 1}</span></div>)}</div></ChartCard><ChartCard title="Resolution mix" detail="How tickets are being closed"><div className="flex flex-col items-center justify-center gap-6 py-8 sm:flex-row"><div className="grid size-40 place-items-center rounded-full" style={{ background: 'conic-gradient(var(--primary) 0 67%, #bae6fd 67% 86%, #dbeafe 86% 100%)' }}><div className="grid size-24 place-items-center rounded-full bg-card text-center"><strong className="text-2xl">94%</strong><span className="text-[10px] text-muted-foreground">resolved</span></div></div><div className="space-y-3 text-sm"><Legend color="bg-primary" label="AI-assisted" value="67%" /><Legend color="bg-sky-200" label="Human-only" value="19%" /><Legend color="bg-blue-100" label="Escalated" value="14%" /></div></div></ChartCard></div><ChartCard title="Customer satisfaction" detail="Average CSAT score by week"><div className="mt-5 grid grid-cols-5 gap-3">{['4.6', '4.7', '4.5', '4.8', '4.9'].map((value, i) => <div key={i} className="rounded-xl bg-muted p-4 text-center"><p className="text-2xl font-semibold">{value}</p><p className="mt-1 text-xs text-muted-foreground">Week {i + 1}</p></div>)}</div></ChartCard></section></SupportShell> }
function Kpi({ icon: Icon, label, value, change }: { icon: typeof BarChart3; label: string; value: string; change: string }) { return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center justify-between"><p className="text-sm text-muted-foreground">{label}</p><Icon size={18} className="text-primary" /></div><p className="mt-3 text-3xl font-semibold">{value}</p><p className="mt-2 text-xs font-semibold text-emerald-600">{change} from previous period</p></div> }
function ChartCard({ title, detail, children }: { title: string; detail: string; children: React.ReactNode }) { return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><h2 className="font-semibold">{title}</h2><p className="mt-1 text-xs text-muted-foreground">{detail}</p>{children}</div> }
function Legend({ color, label, value }: { color: string; label: string; value: string }) { return <div className="flex items-center gap-2"><span className={`size-2.5 rounded-full ${color}`} />{label}<strong className="ml-auto">{value}</strong></div> }
