'use client'
import { useState } from 'react'
import { BookOpen, ChevronRight, FileText, Search } from 'lucide-react'
import { EmptyState, PageHeader, SupportShell } from '@/components/support-shell'
const articles = [{ title: 'Resetting your account password', category: 'Account access', updated: 'Today', reads: 1240 }, { title: 'Understanding your monthly invoice', category: 'Billing', updated: 'Yesterday', reads: 892 }, { title: 'Troubleshooting connection issues', category: 'Technical help', updated: '2 days ago', reads: 761 }, { title: 'Managing workspace members', category: 'Workspace', updated: 'Last week', reads: 544 }, { title: 'How to contact the support team', category: 'Getting started', updated: 'Last week', reads: 421 }]
export default function KnowledgeBasePage() { const [query, setQuery] = useState(''); const filtered = articles.filter((a) => `${a.title} ${a.category}`.toLowerCase().includes(query.toLowerCase())); return <SupportShell active="Knowledge base"><section className="px-4 py-7 sm:px-6 lg:px-8 lg:py-9"><PageHeader eyebrow="Self-service support" title="Knowledge base" description="Keep helpful answers close to your customers and your team." action={<button className="min-h-11 rounded-xl bg-primary px-4 text-sm font-semibold text-primary-foreground">New article</button>} /><div className="mt-8 rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="max-w-xl"><label className="relative block"><Search className="absolute left-3 top-3 text-muted-foreground" size={17} /><input value={query} onChange={(e) => setQuery(e.target.value)} className="h-11 w-full rounded-xl border border-input bg-background pl-10 pr-4 text-sm outline-none focus:border-primary focus:ring-4 focus:ring-primary/10" placeholder="Search articles" aria-label="Search articles" /></label></div><div className="mt-6 flex gap-2 overflow-x-auto pb-1"><button className="whitespace-nowrap rounded-full bg-primary px-4 py-2 text-xs font-bold text-primary-foreground">All articles</button>{['Account access', 'Billing', 'Technical help', 'Workspace'].map((category) => <button key={category} className="whitespace-nowrap rounded-full border border-border px-4 py-2 text-xs font-semibold text-muted-foreground hover:border-primary hover:text-primary">{category}</button>)}</div></div><div className="mt-6 grid gap-4 lg:grid-cols-2">{filtered.length ? filtered.map((article) => <button key={article.title} className="group flex min-h-32 items-start gap-4 rounded-2xl border border-border bg-card p-5 text-left shadow-sm hover:border-primary"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-primary/10 text-primary"><FileText size={19} /></span><span className="min-w-0 flex-1"><span className="block font-semibold group-hover:text-primary">{article.title}</span><span className="mt-2 block text-sm text-muted-foreground">{article.category} · Updated {article.updated}</span><span className="mt-4 block text-xs text-muted-foreground">{article.reads.toLocaleString()} readers</span></span><ChevronRight size={18} className="text-muted-foreground" /></button>) : <div className="lg:col-span-2"><EmptyState title="No articles found" text="Try a different search term." /></div>}</div></section></SupportShell> }
