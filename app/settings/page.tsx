'use client'
import { useState } from 'react'
import { Bell, CreditCard, LockKeyhole, Save, Users } from 'lucide-react'
import { PageHeader, SupportShell } from '@/components/support-shell'
export default function SettingsPage() { const [saved, setSaved] = useState(false); return <SupportShell active="Settings"><section className="px-4 py-7 sm:px-6 lg:px-8 lg:py-9"><PageHeader eyebrow="Workspace controls" title="Settings" description="Manage your profile, notifications, and support workspace." action={saved ? <span className="text-sm font-semibold text-emerald-600">Changes saved</span> : undefined} /><div className="mt-8 grid gap-6 xl:grid-cols-[0.7fr_1.3fr]"><aside className="h-fit rounded-2xl border border-border bg-card p-2 shadow-sm"><button className="flex min-h-11 w-full items-center gap-3 rounded-xl bg-primary/10 px-3 text-left text-sm font-semibold text-primary"><Users size={17} /> Profile</button>{[['Notifications', Bell], ['Team & workspace', Users], ['Security', LockKeyhole], ['Billing', CreditCard]].map(([label, Icon]) => <button key={label as string} className="flex min-h-11 w-full items-center gap-3 rounded-xl px-3 text-left text-sm font-semibold text-muted-foreground hover:bg-muted hover:text-foreground"><Icon size={17} />{label as string}</button>)}</aside><div className="space-y-6"><SettingsCard title="Profile" description="How your name appears to customers and teammates."><div className="grid gap-5 sm:grid-cols-2"><Field label="First name" value="Alex" /><Field label="Last name" value="Morgan" /><Field label="Work email" value="alex@skyline.support" /><Field label="Role" value="Support lead" /></div></SettingsCard><SettingsCard title="Notifications" description="Choose when Skyline should notify you."><Toggle label="New ticket alerts" description="Get notified when a new request arrives." checked /><Toggle label="Daily operations summary" description="Receive a morning summary of your queue." checked /><Toggle label="Customer replies" description="Know when a customer responds to an open ticket." checked={false} /></SettingsCard><SettingsCard title="Plan & billing" description="Your workspace subscription and usage."><div className="flex flex-col gap-4 rounded-xl bg-muted p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold">Professional plan</p><p className="mt-1 text-sm text-muted-foreground">Up to 10 teammates · Renews monthly</p></div><button className="min-h-11 rounded-xl border border-border bg-card px-4 text-sm font-semibold hover:border-primary hover:text-primary">Manage plan</button></div></SettingsCard><button onClick={() => { setSaved(true); setTimeout(() => setSaved(false), 2500) }} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-primary px-5 text-sm font-semibold text-primary-foreground hover:opacity-90"><Save size={16} /> Save changes</button></div></div></section></SupportShell> }
function SettingsCard({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <section className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><h2 className="font-semibold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">{description}</p><div className="mt-6">{children}</div></section> }
function Field({ label, value }: { label: string; value: string }) { return <label className="block"><span className="mb-2 block text-sm font-semibold">{label}</span><input defaultValue={value} className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm outline-none focus:border-primary focus:ring-4 focus:ring-primary/10" /></label> }
function Toggle({ label, description, checked }: { label: string; description: string; checked: boolean }) { return <label className="flex min-h-16 items-center justify-between gap-4 border-b border-border py-3 last:border-0"><span><span className="block text-sm font-semibold">{label}</span><span className="mt-1 block text-xs text-muted-foreground">{description}</span></span><input type="checkbox" defaultChecked={checked} className="size-5 accent-sky-500" /></label> }
